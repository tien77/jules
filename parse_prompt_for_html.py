import os
import re

prompt_file = "design_syntax.txt" # We will supply the text here via stdin
# No wait, I don't have the html files saved directly as unnamed_file_0, wait.
# Actually unnamed_file_0 was an image attachment that the user attached in the background!
# The HTML code is in the USER's prompt directly!
